'use client';

// Mode toggle is no longer used — single aesthetic mode only.
// Kept as a no-op export to avoid breaking any stale imports.
export function ModeToggle({ className }: { className?: string }) {
  return null;
}
